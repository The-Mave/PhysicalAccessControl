<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Página de Registro de Presença</title>
    <!-- Inclua os estilos do Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.16/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="min-h-screen flex items-center justify-center">
        <div class="bg-white p-8 rounded shadow-md w-96">
            <h1 class="text-2xl font-semibold mb-6">Registro de Presença</h1>
            <form action="/registrar-presenca" method="post">
                <!-- Campo para escanear o QR Code -->
                <div class="mb-4">
                    <label for="qrcode" class="block text-gray-600 font-medium mb-2">Escanear QR Code</label>
                    <input type="text" id="qrcode" name="qrcode" class="w-full px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring focus:ring-blue-300" required>
                </div>
                <!-- Campo para confirmar a geolocalização -->
                <div class="mb-4">
                    <label class="block text-gray-600 font-medium mb-2">Confirmar Geolocalização</label>
                    <div class="flex items-center space-x-2">
                        <input type="checkbox" id="geolocalizacao" name="geolocalizacao" class="w-5 h-5 border border-gray-300 rounded-md focus:outline-none focus:ring focus:ring-blue-300">
                        <label for="geolocalizacao" class="text-gray-700">Confirmo a geolocalização</label>
                    </div>
                </div>
                <!-- Botão para registrar presença -->
                <div class="mt-6">
                    <button type="submit" class="w-full bg-blue-500 text-white py-2 px-4 rounded-md hover:bg-blue-600 focus:outline-none focus:ring focus:ring-blue-300">Registrar Presença</button>
                </div>
            </form>
        </div>
    </div>
</body>
</html>